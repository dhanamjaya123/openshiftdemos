apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helochart.fullname" . }}-test-connection"
  labels:
    {{- include "helochart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helochart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
